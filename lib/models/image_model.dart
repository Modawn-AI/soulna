import '../utils/package_exporter.dart';

class ImageModel {
  Color linear1;
  Color linear2;
  String image;
  String text;

  ImageModel(
      {required this.linear1, required this.linear2, required this.image,required this.text});
}